@model IEnumerable<AssetMonitorDataAccess.Models.AlarmTagConfig>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_ConfigurationLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tag)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AlarmType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ActivationTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tag.Tagname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AlarmType.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ActivationTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
